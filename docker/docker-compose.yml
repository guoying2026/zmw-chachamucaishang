version: '3'
services:
    zmw-chachamucaishang:
        image: registry.cn-beijing.aliyuncs.com/jryhd/zmw-pingfen:latest
        command: node
        environment:
            - SITE_DOMAIN=localhost
            - WWWROOT=/wwwroot
        ports:
            - 80:3000
